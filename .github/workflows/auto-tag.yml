name: Auto-Tag

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

on:
  pull_request:
    types: [ "closed" ]

jobs:
  update_tag:
    uses: rwunderer/renovate/.github/workflows/reusable-auto-tag.yaml@main
    with:
      regex-pattern: "KUBECTL_VERSION=(v\\d+\\.\\d+\\.\\d+)"
      tag-prefix: ""
    secrets: inherit
